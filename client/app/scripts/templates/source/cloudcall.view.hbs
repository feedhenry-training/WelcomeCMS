 {{! handlebars template, will be compiled by the grunt task. The compiled version will be used in the app to improve perfermance}}
 <div class="content_view">
  <div class="container">
    <h1 class="appheading">{{header}}</h1>
    <p>{{text}}</p>
    <h2>Name</h2>
    <p class="appdata appdata_name">{{page1Name}}</p>
    <p class="appdata appdata_address">{{page1Address}}</p>
     <p class="appdata" ><img src="{{page1Img}}" /></p>
  </div>
</div>